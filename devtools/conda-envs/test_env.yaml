name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - scipy
  - matplotlib
  - pytest
  - pytest-cov
  - codecov
  - jupyter
  - nbconvert
  - nbformat
  - ipykernel
  - ipython
  - jupyterlab
  - jupyterlab_widgets
  - nodejs
  - jupyterlab_code_formatter


    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  #- pip:
  #  - codecov

